[project]
name = "travel-agent-sensitive-masker"
version = "0.1.0"
description = "企业级智能旅行助手统一敏感信息脱敏工具"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/travel_agent_sensitive_masker"]
