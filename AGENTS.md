1. 在新增或修改任何内容前，必须先查看与该工作相关的现有目录、文件和配置，并以实际内容为依据进行增量修改；不得只凭记忆假设现有工程结构或覆盖用户已有实现。
2. 所有新增代码文件必须在文件开头使用中文注释说明文件职责，并简要列出其后定义的函数或类及其功能；所有 README 及其他 Markdown（`.md`）文档均使用中文编写。
3. 遇到会实质影响架构、数据库 schema、模型接入、成本、安全或上线行为的前置决策时，必须及时向用户说明影响并请求需求澄清；例如创建 pgvector 内容表前，必须确认 embedding Provider、模型、向量维度与版本。未获得足够明确的用户需求前，不得自行假设或开始相关代码实现；即使此前已经提过，也应在临近实施该决策点时再次主动提醒和确认。
